services:
  pdf-generator:
    build: .
    restart: unless-stopped
    ports:
      - "${HOST_PORT:-3000}:${PORT:-3000}"
    env_file:
      - .env
    volumes:
      - pdf-output:/app/output
    deploy:
      resources:
        limits:
          memory: 800m

volumes:
  pdf-output:
